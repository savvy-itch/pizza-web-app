export const pizzasUrl =  'api/pizzas';
export const ingredientsUrl = 'api/ingredients';
export const numOfSkeletons = 4;

// function getApiUrl(endpoint) {
//   const isProduction = window.location.hostname.includes('prog-pizza-website.netlify.app');
//   const baseUrl = isProduction ? '' : 'http://localhost:3000/';
//   console.log(`${baseUrl}${endpoint}`)
//   return `${baseUrl}${endpoint}`;
// }